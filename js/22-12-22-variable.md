## 변수란?
변수는 하나의 값을 저장하기 위한 수단이다.

```javascript
var userId; // 변수 선언 : let, const, var 키워드를 통해 변수를 선언한다 & 변수 초기화 : 변수가 선언된 이후 최초로 값을 할당하는 것
console.log("userId 값 : " + userId); // undifined를 할당하여 초기화한다
userId = "00102"; // 변수 할당
userId = "11203"; // 값의 재할당
```
